#include "main.h"
#include <stdio.h>

/**
 * Description: mul - Multiplies two integers.
 * @a: First integer to multiply
 * @b: Second integer to multiply
 * Return: 0 success
 */

int mul(int a, int b)
{
	return (a * b);
}
