#include "main.h"
#include <stdio.h>

/**
* whatsmyname - Prints the name of file even if renamed
* @argc: argument count
* @argv: argument vector
* return: (0) success
*/

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
