#include "main.h"
#include <stdio.h>

/**
 * _puts - Refer to Description
 * @str: String to be printed
 * Description: Function prints a string followed by new line to stdout.
 * Return: (0) success
 */

void _puts(char *str)
{
	_puts(str);
}
